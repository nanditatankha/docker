apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworld-py.fullname" . }}
  labels:
    {{- include "helloworld-py.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloworld-py.selectorLabels" . | nindent 4 }}
